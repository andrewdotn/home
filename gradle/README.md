# Grade build stats plugin

Records stats for all builds to `~/.gradle/build_history.json`

To install:

    gradlew publish

And make sure `cloudhomedir` has linked
`~/.gradle/init.d/buildstats.gradle`.
